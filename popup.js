document.addEventListener('DOMContentLoaded', function () {
    var contentFrame = document.getElementById('contentFrame');

    // Defina o tamanho do iframe conforme necessário
    contentFrame.style.width = '800px';
    contentFrame.style.height = '600px'; // Altura em pixels

    // Você pode lidar com eventos de carregamento do iframe aqui, se necessário
});


